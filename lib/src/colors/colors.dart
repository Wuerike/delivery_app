import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const accentColor = Color.fromRGBO(10, 31, 68, 1.0);
  static const yellow = Color.fromRGBO(255, 204, 0, 1.0);
  static const greyColor = Color.fromRGBO(184, 187, 198, 1.0);
  static const greyColor2 = Color.fromRGBO(209, 209, 214, 1.0);
  static const pink = Color.fromRGBO(255, 45, 85, 1.0);
  static const white = Colors.white;
  static const black = Colors.black;
  static const orange = Color.fromRGBO(255, 140, 0, 1.0);

  // Customs
  static const orangeWithHalfOpacity = Color.fromRGBO(255, 140, 0, 0.5);

  // Components
  static const fbButtonColor = Color.fromRGBO(0, 122, 255, 1.0);

  // Backgrounds
  static const bgGreyPage = Color.fromRGBO(247, 248, 250, 1.0);
  static const bgInputs = Color.fromRGBO(142, 142, 147, 1.2);
}
